/* Write an application that displays the factorial for every integer value from 1 to 10. 
A factorial of a number is the product of that number multiplied by each positive interger 
lower than it. For example, 4 factorial is 4 * 3 * 2 * 1, or 24. */

import java.util.Scanner;
    public class Main {
        public static void main (String []args) {
            
            int factorial = 1;
            
            System.out.println ("Please enter an integer value from 1 to 10.");
            Scanner keyboard = new Scanner (System.in);
            int integer = keyboard.nextInt();
            
            for (int i = 1; i <= integer ; i++) {
                factorial = factorial * i;
            }
            System.out.println("The integer number is: " + integer);
            System.out.println("The factorial number is: " + factorial);
        }
        
    }
Footer
